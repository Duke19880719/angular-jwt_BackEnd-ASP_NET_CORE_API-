{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Key": "ThisIsAVeryVerySecretLongKeyForJWT22452001!",
    "Issuer": "AspNetCoreApi",
    "Audience": "AngularApp"
  }
}

//🔹Key 是用來 簽發 & 驗證 JWT 簽名（Signature） 的密碼文字（secret key）。
//這個值非常重要，因為伺服器會用它來確保 JWT 真的是合法發出的，而不是別人偽造的。
//✅ 如何定義 Key
//1. 至少要有足夠長度（如 32 個以上字元），這樣加密強度才夠。
//2. 不要用簡單的字串（像 "123"、"abc"），這會降低安全性。
//3. 在 正式環境 不要直接硬編在 appsettings.json 裡，
//而是透過環境變數或密碼管理服務（例如 Azure Key Vault / AWS Secrets Manager 等）。


//🔹Issuer（發行者）
//它代表 是哪一個伺服器或系統發出了這個 Token。
//通常是你的身份驗證伺服器或 API 的名稱、URI。
//Token 上的 iss 值會由接收端驗證，確保 Token 是從可信的來源發出。


//🔹 Audience（受眾）
//它代表 這個 Token 是給哪個系統或應用使用的。
//當後端 API 接收到 JWT token 時會檢查 aud 是否是它自己預期的受眾。
//如果不匹配，後端會拒絕這個 token，避免這個 token 被用在 不適合的服務。

